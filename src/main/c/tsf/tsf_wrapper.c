/*
 * TSF wrapper — compiles TinySoundFont as a shared library.
 *
 * Include tsf.h with TSF_IMPLEMENTATION so the single-header library
 * generates its function bodies in this translation unit.
 */
#define TSF_IMPLEMENTATION
#include "tsf.h"
